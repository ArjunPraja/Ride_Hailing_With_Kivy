<DriverRidesScreen>:
    name: "driver_rides"
    rides_container: rides_container

    MDBoxLayout:
        orientation: "vertical"
        spacing: dp(15)
        padding: dp(15)

        MDLabel:
            text: "🚖 My Rides"
            halign: "center"
            font_style: "H5"
            bold: True
            size_hint_y: None
            height: self.texture_size[1] + dp(20)

        ScrollView:
            size_hint_y: 1
            MDBoxLayout:
                id: rides_container
                orientation: "vertical"
                spacing: dp(10)
                padding: dp(10)
                adaptive_height: True
                size_hint_y: None

        MDRaisedButton:
            text: "⬅ Back"
            md_bg_color: 1, 0, 0, 1
            size_hint_y: None
            height: dp(50)
            on_release: app.root.current = "driver_menu"  # Change this to your driver dashboard screen name
